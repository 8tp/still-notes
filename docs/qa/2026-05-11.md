# qa report — 2026-05-11

## verified

- branch: `overnight/2026-05-11-hardening`
- local command: `JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home ./gradlew --no-daemon :app:assembleDebug :app:testDebugUnitTest --stacktrace`
- result: green
- pact check: zero manifest permissions, no network-adjacent permissions, no firebase/gms/analytics dependency

## work reviewed

- root pact link added to the README
- build workflow added
- markdown block tests added for headings, lists, blockquotes, fenced code, and horizontal rules
- markdown inline tests added for bold, italic, inline code, and links
- tag extraction now ignores paired inline-code spans
- tag extraction now follows parser line-level fenced-code rules, including literal triple backticks inside fenced content; prior fenced-code tag-leak risk is fixed and stale
- multiline inline-code spans and markdown link destinations are masked before tag extraction, so code/link-target `#tags` do not leak into filters
- repo pact verifier checks source, merged, and packaged manifests, and CI now runs debug lint with the build and JVM tests
- share and single-note export now flush the debounced editor save before reading note bytes or deriving export metadata

## risks

- markdown renderer remains deliberately small and does not cover tables, HTML, footnotes, math, or syntax highlighting
- share/export freshness is code-reviewed and build-validated; there is no Compose UI smoke for rapid edit-then-share/export yet

## next improvements

- add test fixtures for malformed markdown delimiters and adjacent emphasis markers
- add import/export repository tests around note filenames and duplicate title handling
