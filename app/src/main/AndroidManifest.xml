<?xml version="1.0" encoding="utf-8"?>
<!--
    Android manifest for Still Notes.

    Privacy posture (matches still-launcher):
    - No INTERNET permission.
    - No QUERY_ALL_PACKAGES.
    - No analytics, telemetry, or background services.
    - Notes are stored as plain .md files in app-private storage.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_still_notes_launcher"
        android:roundIcon="@drawable/ic_still_notes_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.StillNotes"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Lets other apps share text into still-notes via the Android share sheet. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>
</manifest>
